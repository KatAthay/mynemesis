const db = require('')
const nemesisController = {};

nemesisController.getTweets = (req, res, next) => {
	//const query = mongoSyntax goes here

	//send query to populate search terms
	db.query(query)
	.then(data => {
		
	})
}